/**
 * Seeded catalogue labels (from apps/api prisma seed).
 * Happy-path specs will use these; smoke does not need them yet.
 */
export const SEEDED = {
  skillName: 'Basic Car Maintenance',
  languageName: 'English',
} as const
